# El arbol de dependencias de Expo/React Native suele traer conflictos de peer
# deps entre react, react-dom y react-native que no son reales en la practica.
# Sin esto, "expo install" falla al correr npm install por dentro.
legacy-peer-deps=true
